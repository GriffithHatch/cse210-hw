int Timer(){
        var starttime = DateTime.Now;
        var stoptime = starttime.AddSeconds(time);
        while (starttime != stoptime){
            return 0;
        }
        return 1;
    }
int count = Timer()
int guh;
While(count != 1){
    guh += 1
    Thread.Sleep(1000)
    
}